# Session Handoff — 2026-04-03

## What Was Done This Session

### 1. Home Landing Page (`src/static/home.html`)
- Created a new landing page at `/` with full DND military branding
- 3D floating Canadian flag (SVG with gradients and shine animation)
- OODA Loop framework section (4 cards: Sense, Make Sense, Decide, Act)
- Platform Capabilities grid (6 cards with SVG icons)
- Intel feed ticker (22 feeds, scrolling marquee)
- Bottom CTA "Ready for Deployment"
- Stats counter animation (57 feeds, 31 minerals, 9311 transfers, 186 nations)
- Classification bars top/bottom ("UNCLASSIFIED // OSINT")
- Right-side navigation menu linking to all 7 dashboard tabs
- `src/main.py` updated: `/` serves home.html, `/dashboard` serves index.html

### 2. Theme Unification (dashboard `src/static/index.html`)
**Phase 1 — Foundation (all tabs):**
- Swapped fonts: Outfit/IBM Plex Sans → Barlow Condensed/DM Sans
- Updated root CSS variables (darker bg `#06090f`, white-based borders, DND red `#D80621`)
- Added atmospheric effects (80px grid overlay, red/cyan radial glow, 16s scan line)
- Added classification bars top/bottom
- Added right-side navigation menu with tab switching + sync
- Restyled header: sticky translucent bar, CMCT branding (was "PSI CONTROL TOWER")
- Restyled nav tabs: monospace, underline-active (was rounded pill tabs)
- Removed all border-radius (sharp military edges)
- Removed glass-morphism (solid `#111620` card backgrounds)
- Updated all hardcoded JS colors (Chart.js defaults, Cesium globes, flag SVG)

**Phase 2 — Insights Tab Polish:**
- Briefing headers: numbered circles → `SECTION 01` tag + large title pattern
- Alerts: thinner 2px left border, tighter spacing, hover to `#161c2a`
- Buttons: red CTA replacing cyan gradient
- Badges: monospace font
- Context boxes: solid bg with border
- Grid gaps tightened (20px → 4px)
- Removed Data Freshness banner (not needed)

### 3. Light/Dark Theme Toggle
- Added `body.light` CSS class with full light palette override
- Toggle switch in header (Dark | slider | Light) on both pages
- Persists via `localStorage` key `cmct-theme`
- Light mode: hides atmospheric effects, white backgrounds, dark text, deeper accents
- Keyboard accessible (Space/Enter)

### 4. Desaturated "Signal" Color Palette
Replaced all bright accent colors across both pages (CSS + JS + inline HTML):

| Role | Before (bright) | After (desaturated) |
|------|-----------------|---------------------|
| Green | `#34D399` / `#10b981` | `#6b9080` (sage) |
| Amber | `#FBBF24` / `#f59e0b` | `#a89060` (ochre) |
| Purple | `#8b5cf6` | `#6b6b8a` (slate) |
| Blue (NATO) | `#3b82f6` | `#5a7a9b` (steel) |
| Teal | `#14b8a6` | `#5a8a80` (muted teal) |
| Red | `#ef4444` | `#D80621` (DND red) |

Cyan (`#00d4ff`) and DND Red (`#D80621`) are the two signal colors. Everything else recedes.

### 5. Globe Layer Control System
- 8 toggleable layers: Mining, Processing, Components, Platforms, Shipping Routes, Chokepoints, Canadian Ports, Flow Arcs
- `GLOBE_LAYERS` config array drives both panel UI and entity tagging
- Collapsible panel: layers-stack icon (top-right) expands to 220px toggle panel
- Every Cesium entity tagged with `layerId` property
- Toggle = `entity.show = true/false` — no API re-fetch
- Layer state persists across mineral switches
- `addFlowArc()` updated to accept and propagate `layerId`
- Collapsible legend sections (Supply Chain Nodes + Shipping Route Risk)

### 6. Diamond Markers (attempted, reverted)
- Attempted to render Tier 1/2 facilities as diamond-shaped billboard markers
- CesiumJS 1.119 crashes with `RangeError: Invalid array length` on billboard rendering
- **Reverted** to point markers with white outlines
- Coordinates are verified as exact facility locations (not country centroids)
- Known issue: CesiumJS 1.119 billboard with canvas/data-URL images crashes in `createPotentiallyVisibleSet`

### 7. CMCT Branding
- Dashboard header: maple leaf icon + "CMCT" + "Defence Supply Chain Intelligence"
- CMCT logo is now a link back to home page (`/`)
- Consistent branding across home and dashboard

## Files Changed
- `src/static/home.html` — NEW (landing page, ~520 lines)
- `src/static/index.html` — MODIFIED (~500 lines changed across CSS, HTML, JS)
- `src/main.py` — MODIFIED (5 lines: home route + dashboard route)
- `data/` — NEW directory (asset files: logos, images, forecast data)
- `docs/superpowers/specs/2026-04-02-globe-layer-control-design.md` — NEW
- `docs/superpowers/plans/2026-04-03-globe-layer-control.md` — NEW

## Known Issues / Technical Debt
1. **CesiumJS billboard crash** — cannot use custom canvas/data-URL billboard images in Cesium 1.119. Diamond markers will need Cesium upgrade or a different approach (e.g., custom primitive, point with CSS overlay)
2. **`buildDiamondImage()` function** — still in codebase but unused after revert. Can be removed or kept for future Cesium upgrade
3. **Light theme** — functional but not deeply tested across all 7 tabs. Arctic/Supply Chain globes (Cesium/Leaflet) don't respond to CSS variables
4. **Inline styles** — many hardcoded `rgba(...)` colors in HTML inline styles were bulk-replaced; some edge cases in deeply nested JS-generated HTML may still have old values
5. **Risk Taxonomy scores** — mostly seeded baselines with ±5-8 random noise. Only Categories 1 (FOCI) and 3 (Manufacturing) use real DB data

## What's Next (Recommended Priority)
1. Continue theme unification to remaining tabs (Arctic, Canada Intel, Supply Chain sub-tabs, Data Feeds, Compliance)
2. Deep-dive remaining 29 minerals to Cobalt depth
3. Explore alternative to billboard for diamond markers (Cesium PointPrimitive with custom shader, or upgrade to Cesium 1.120+)
4. Full light theme QA pass across all tabs
5. French translation of dynamic content
